<UserControl x:Class="Imperial_Commander_Editor.DefaultWindowStyle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Imperial_Commander_Editor"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
						 TextElement.FontWeight="Medium"
						 TextElement.FontSize="14"
						 FontFamily="{materialDesign:MaterialDesignFont}"
             d:DesignHeight="200" d:DesignWidth="400" Name="dws">

	<Border BorderBrush="MediumPurple" BorderThickness="2">
		<GroupBox Header="{Binding header, ElementName=dws}">
			<ContentPresenter Content="{Binding AdditionalContent, ElementName=dws}" />
		</GroupBox>
	</Border>
</UserControl>
